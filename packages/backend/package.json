{
  "name": "@poe2-overlord/backend",
  "private": true,
  "version": "0.1.0",
  "description": "Rust backend for POE2 Overlord",
  "type": "module",
  "scripts": {
    "format": "cargo fmt",
    "format:check": "cargo fmt -- --check",
    "lint": "cargo clippy --all-targets --all-features -- -D warnings",
    "test": "cargo test",
    "test:verbose": "cargo test --verbose",
    "test:watch": "cargo watch -x test",
    "check": "cargo check",
    "build": "cargo build",
    "clean": "cargo clean"
  }
}
