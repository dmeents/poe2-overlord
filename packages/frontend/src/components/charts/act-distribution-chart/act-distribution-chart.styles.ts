import { chartStyles } from '../distribution-donut-chart/distribution-donut-chart.styles';

export const actChartStyles = {
  ...chartStyles,
  chartWrapper: 'relative flex items-center justify-center h-48',
  totalRow: 'font-medium',
};
